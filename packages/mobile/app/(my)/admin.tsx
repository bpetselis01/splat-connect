import { SectionStub } from '../../components/ui/SectionStub'
export default function MyAdmin() { return <SectionStub title="Admin" blurb={"Review tables don't fit a phone. These open on the web, signed in."} /> }
