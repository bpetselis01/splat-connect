// packages/mobile/app/(my)/organisation/recycling.tsx
// The board routes this as (org)/recycling. There is no (org) group — the
// organisation screens live under (my)/organisation beside toys and orders.
import { IntakeScreen } from '../../../components/recycling/intake-screen'

export default function OrgRecycling() {
  return <IntakeScreen />
}
